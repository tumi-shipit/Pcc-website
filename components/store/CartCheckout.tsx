"use client";
import Image from "next/image";
import Link from "next/link";
import { useState } from "react";
import { useStoreCart } from "@/components/store/StoreCart";

export default function CartCheckout({ checkoutOpen }: { checkoutOpen: boolean }) {
  const cart=useStoreCart(); const [customerName,setName]=useState(""); const [customerEmail,setEmail]=useState(""); const [customerPhone,setPhone]=useState(""); const [loading,setLoading]=useState(false); const [error,setError]=useState("");
  const hasService=cart.lines.some((line)=>line.service);
  async function pay(){setLoading(true);setError("");try{const response=await fetch("/api/store/checkout",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({items:cart.lines.map((line)=>({productId:line.productId,quantity:line.quantity,option:line.option})),customerName,customerEmail,customerPhone})});const body=await response.json() as {redirectUrl?:string;error?:string};if(!response.ok||!body.redirectUrl)throw new Error(body.error||"Could not start payment.");window.location.assign(body.redirectUrl);}catch(e){setError(e instanceof Error?e.message:"Could not start payment.");setLoading(false)}}
  if(!cart.lines.length)return <div className="py-24 text-center"><h1 className="text-4xl font-black">Your bag is empty.</h1><p className="mt-3 text-slate-500">Choose equipment or clubwear to begin.</p><Link href="/store" className="mt-7 inline-block rounded-full bg-slate-950 px-7 py-3.5 text-sm font-black text-white">Continue shopping</Link></div>;
  return <div className="grid gap-10 lg:grid-cols-[1fr_25rem]"><section><h1 className="text-4xl font-black tracking-tight sm:text-5xl">Your bag</h1><div className="mt-8 divide-y divide-slate-200 border-y border-slate-200">{cart.lines.map((line)=><article key={`${line.productId}-${line.option||""}`} className="grid grid-cols-[7rem_1fr] gap-5 py-6"><div className="relative aspect-square bg-[#f5f5f3]">{line.imageUrl&&<Image src={line.imageUrl} alt={line.name} fill sizes="112px" className="object-contain p-2"/>}</div><div><div className="flex justify-between gap-4"><div><Link href={`/store/${line.slug}`} className="font-black hover:underline">{line.name}</Link>{line.option&&<p className="mt-1 text-xs text-slate-500">Size: {line.option}</p>}</div><p className="font-black">R{(line.price*line.quantity).toLocaleString("en-ZA")}</p></div><div className="mt-4 flex items-center gap-4">{!line.service&&<label className="text-xs font-bold">Qty <input type="number" min="1" max={line.maxQuantity??20} value={line.quantity} onChange={(e)=>cart.update(line.productId,line.option,Number(e.target.value)||1)} className="ml-2 w-16 rounded-lg border border-slate-300 px-2 py-2"/></label>}<button type="button" onClick={()=>cart.remove(line.productId,line.option)} className="text-xs font-black underline">Remove</button></div></div></article>)}</div></section><aside className="h-fit bg-[#f5f5f3] p-6 lg:sticky lg:top-28"><h2 className="text-2xl font-black">Order summary</h2><div className="mt-5 flex justify-between border-b border-slate-300 pb-5"><span>{cart.count} items</span><strong>R{cart.total.toLocaleString("en-ZA")}</strong></div><div className="mt-6 space-y-4"><input aria-label="Full name" placeholder="Full name" value={customerName} onChange={(e)=>setName(e.target.value)} className="w-full rounded-xl border border-slate-300 bg-white px-4 py-3"/><input aria-label="Email address" type="email" placeholder="Email address" value={customerEmail} onChange={(e)=>setEmail(e.target.value)} className="w-full rounded-xl border border-slate-300 bg-white px-4 py-3"/><input aria-label="Phone number" placeholder="Phone number" value={customerPhone} onChange={(e)=>setPhone(e.target.value)} className="w-full rounded-xl border border-slate-300 bg-white px-4 py-3"/></div>{error&&<p className="mt-4 text-sm font-bold text-red-700">{error}</p>}<button type="button" disabled={!checkoutOpen||loading||!customerName||!customerEmail||!customerPhone} onClick={()=>void pay()} className="mt-6 w-full rounded-full bg-slate-950 px-5 py-4 text-sm font-black text-white disabled:cursor-not-allowed disabled:bg-slate-300">{checkoutOpen?loading?"Opening secure payment…":"Pay securely online":"Online ordering opens soon"}</button><p className="mt-4 text-xs leading-5 text-slate-500">{hasService?"PCC does not receive or keep your card details. For profile services, PCC contacts you after payment to collect and approve the correct portrait.":"PCC does not receive or keep your card details. PCC will contact you to confirm collection or delivery."}</p></aside></div>;
}
